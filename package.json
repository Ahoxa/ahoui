{
  "name": "ahoui",
  "author": "Ahoxa",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/react build && bun run --cwd packages/solid build",
    "story:react": "cd packages/react && bunx storybook dev -p 6006 "
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.6",
    "@types/bun": "latest"
  },
  "module": "index.ts",
  "type": "module"
}
